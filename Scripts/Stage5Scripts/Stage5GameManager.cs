using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Stage5GameManager : MonoBehaviour
{
    public static bool isCandleBright = false; //비밀의 방의 향초를 켰는지
    public static bool isLegendPlaying = false; //레테 신 관련 설화 재생 여부
    public static bool GettingFlower =false; //뱃사공에게 꽃을 주었는지
    public static bool GettingCoin =false; //뱃사공에게 뱃삯을 주었는지
    public static bool RidingBoat = false; //뱃사공에게 뱃삯을 주었는지
    public static bool isUndertheRiver =false; //물에 빠졌는지
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
